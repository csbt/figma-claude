# Prompt — Giai đoạn H (vòng lặp)

## Prompt kiểm
```
Đọc CLAUDE.md + PROJECT.md và guides/H-responsive/instructions.md, rồi thực thi.
Dùng Playwright MCP chụp từng trang ở các viewport 360 / 768 / 1024 / 1440.
Liệt kê lỗi reflow: scroll ngang, vỡ grid, chữ/ảnh tràn, nav chưa thành hamburger, touch target nhỏ.
KHÔNG sửa gì ở giai đoạn này — chỉ báo cáo để tôi xem.
```

## Prompt sửa (lặp)
```
Sửa các lỗi reflow sau theo Reflow Rules trong CLAUDE.md, TUYỆT ĐỐI không thay đổi giao diện desktop:
<dán danh sách lỗi>.
Test thêm với nội dung dài (text/tên dài) để chắc không vỡ. Chụp lại các breakpoint để xác nhận.
Cập nhật PROJECT.md (tick H) khi sạch.
```
